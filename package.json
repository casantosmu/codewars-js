{
  "name": "codewars-js",
  "type": "module",
  "scripts": {
    "codegen": "node ./scripts/generate-readme.js && prettier ./README.md --write",
    "lint": "eslint . && prettier . --check",
    "lint:fix": "eslint --fix . && prettier . --write"
  },
  "devDependencies": {
    "@casantosmu/eslint-config": "^0.0.0",
    "prettier": "3.1.1"
  }
}
