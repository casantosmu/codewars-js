{
  "name": "codewars-js",
  "type": "module",
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "generate-readme": "node scripts/generate-readme.js && prettier README.md --write",
    "generate-challenge": "node scripts/generate-challenge.js && npm run generate-readme",
    "lint": "eslint . && prettier . --check",
    "lint:fix": "eslint --fix . && prettier . --write"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "eslint": "^9.15.0",
    "eslint-config-xo": "^0.46.0",
    "eslint-plugin-unicorn": "^56.0.1",
    "globals": "^15.12.0",
    "prettier": "3.4.1"
  }
}
